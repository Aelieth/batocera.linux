# SNES-HD assets

Project-owned art. Iterate here. The image still installs from
`package/batocera/sneshd/sneshd-plymouth/` until we point the package
at this tree.

These are the **current** stills and the recovered `snes-load` theme.
Plymouth last-frame and loading-image match is later work.

| Directory | What |
|---|---|
| [`logos/`](logos/) | Product / ES still (`logo.png`) and Plymouth fade-in wordmark (`plymouth-logo01.png`) |
| [`splash/`](splash/) | Batocera loading stills (`splash.png`, `boot-logo*.png`) used today |
| [`plymouth/snes-load/`](plymouth/snes-load/) | Complete `snes-load` theme (dots → silhouette → logo) |

Do not put ROMs, `SNES/` archive dumps, or `*.cpio` here.
